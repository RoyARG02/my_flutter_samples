//* importing flutter libraries to be used in this project
import 'package:flutter/material.dart';
//* importing user-made package to be used in the project('./' means from the root of project)
import './compart/pakclass.dart';

// * Every flutter application starts executing from the pretty familiar 'main' function,
// * which calls the name-obvious 'runApp' method, taking a 'Widget' (read as Component of the App)
// * as the parameter

void main() => runApp(new MyApp());
